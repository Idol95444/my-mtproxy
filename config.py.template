PORT = 853

USERS = {
    "user1": "__BASE_SECRET__",
}

# Твой домен — Caddy выпустит для него LE-сертификат, alexbers заберёт его длину
TLS_DOMAIN = "__DOMAIN__"

# Только FakeTLS — минимум детектируемости
MODES = {"classic": False, "secure": False, "tls": True}

# Прямое подключение к DC Telegram (порт 443, все 5 DC работают)
# Middle proxy (port 8888) ломает DC2/DC4 на многих VPS
USE_MIDDLE_PROXY = False

# Uncomment after registering proxy in @MTProxybot (/newproxy)
# AD_TAG = "__AD_TAG__"
